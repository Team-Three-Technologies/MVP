import { DipPresenter } from './dip-presenter';

describe('DipPresenter', () => {
  it('should create an instance', () => {
    const directive = new DipPresenter();
    expect(directive).toBeTruthy();
  });
});
